services:
  - type: web
    name: oral-exam-board
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATA_DIR
        value: /var/data
    disk:
      name: oral-exam-board-data
      mountPath: /var/data
      sizeGB: 5
